rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /outlierGames/{gameId} {
      allow read: if true;
      allow write: if false;
    }

    match /scanRuns/{runId} {
      allow read: if true;
      allow write: if false;
    }

    match /meta/{docId} {
      allow read: if docId in ["publicStatus", "homepageHighlights", "archiveSnapshot"];
      allow write: if false;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
